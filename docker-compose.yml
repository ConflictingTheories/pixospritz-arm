version: '3.8'

services:
  buildroot:
    build: .
    volumes:
      # Mount the ENTIRE project directory
      - .:/home/builder/work
    working_dir: /home/builder/work
    tty: true
    stdin_open: true
    user: builder
